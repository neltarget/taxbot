# Local development: point to the FastAPI backend
VITE_API_URL=http://localhost:8000

# Production (Render single-service): leave empty so requests go to same origin
# VITE_API_URL=

# Production (Vercel + Render): leave empty — Vercel rewrites /api/* to the backend
# VITE_API_URL=
